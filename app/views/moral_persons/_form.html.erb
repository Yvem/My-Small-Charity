<%= form_for(@moral_person) do |f| %>
  <% if @moral_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moral_person.errors.count, "error") %> prohibited this moral_person from being saved:</h2>

      <ul>
      <% @moral_person.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
