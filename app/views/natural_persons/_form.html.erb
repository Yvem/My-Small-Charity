<%= form_for(@natural_person) do |f| %>
  <% if @natural_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@natural_person.errors.count, "error") %> prohibited this natural_person from being saved:</h2>

      <ul>
      <% @natural_person.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
